# Passenger_Density_Prediction_LSTM
A python based LSTM model, which will predict the passenger presence in railway station using LSTM model. Technology used are Jupyter Notebook, Anaconda and Python.
